[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "monte-carlo-symbolic-regression"
version = "0.1.0"
dependencies = [
    "numpy",
    "sympy",
    "datasets",
]

[tool.setuptools.packages.find]
where = ["src"]